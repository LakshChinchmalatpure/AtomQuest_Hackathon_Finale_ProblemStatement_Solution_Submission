# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/atomquest

# JWT secret (pick a strong random string)
SESSION_SECRET=change-me-to-a-random-secret-string

# Server port (default 5000)
PORT=5000

# Frontend dev server port (default 3000)
VITE_PORT=3000
